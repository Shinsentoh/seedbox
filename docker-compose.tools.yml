version: "3.7"
services:

    ast:
        build: https://github.com/mAlfosea/altered-sealed-tool#main
        container_name: ast
        restart: unless-stopped
        environment:
            - NODE_ENV=production
        labels:
            - "traefik.enable=true"
            - "traefik.http.services.ast.loadbalancer.server.port=3000"
            - "traefik.http.routers.ast.rule=Host(`altered.${TRAEFIK_DOMAIN}`)"
            - "traefik.http.routers.ast.service=ast"
        depends_on:
            - traefik